[package]
name = "benchmark"
version.workspace = true
edition.workspace = true

[dev-dependencies]
criterion.workspace = true
storage = { path = "../storage" }
query = { path = "../query" }
core = { path = "../core" }

[[bench]]
name = "grid_bench"
harness = false
